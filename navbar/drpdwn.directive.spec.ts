import { DrpdwnDirective } from './drpdwn.directive';

describe('DrpdwnDirective', () => {
  it('should create an instance', () => {
    const directive = new DrpdwnDirective();
    expect(directive).toBeTruthy();
  });
});
